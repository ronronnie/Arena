/**
 * Database seed.
 *
 * PLACEHOLDER — Prompt 1 writes the real one. It matters more than it sounds: Prompts
 * 5-7 build the voting surface against seeded fixture videos, before the upload pipeline
 * exists. A seed that produces empty pairs makes the most important screen in the app
 * un-buildable.
 */
async function main() {
  throw new Error(
    'No seed yet. Prompt 1 defines the domain model and the fixture data this depends on.',
  );
}

main().catch((error: unknown) => {
  console.error(error);
  process.exit(1);
});
